<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>RSF - <%= title %></title>
    <meta name="keywords" content="responsive,sprites,sprite,images,framework,font-awesome">
    <meta name="description" content="Responsive Sprites Framework is unique output of sprites images in the way thats way more precisious (no loosing pixels). With increased precision, RSF images are full responsive. The nature of implemented RSF images is very similar to HTML IMG tags. They are easy to implement, and the way they are displayed irresistibly resembles the popular font-avesome.">
    <meta name="og:title" property="og:title" content="Responsive Sprites Framework Creation Engine">
    <meta name="robots" content="index, follow">

    <%
        //import stylesheets
        if (css.length > 0){
            css.forEach(element => {
    %>
                <link rel="stylesheet" href="/stylesheets/<%= element %>">
    <%
            });
        }
    %>

    <%
        //import FE scripts
        if (js.length > 0){
            js.forEach(element => {
    %>
                <script type="text/javascript" src="/javascripts/<%= element %>"></script>
    <%
            });
        }
    %>

    <!-- Global site tag (gtag.js) - Google Analytics -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=UA-64358731-2"></script>
    <script>
        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
        gtag('js', new Date());
        gtag('config', 'UA-64358731-2');
    </script>

</head>

<body>

    <div class="loader">
        <div class="loader__middle">
            <div class="loader__inner">
                <img src="/images/ajax-loader.gif" alt="">
            </div>
        </div>
    </div>

    <div class="container">

        <%- include('./nav'); -%>

        <main>
            <div class="main__inner">
